<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        M-16-A: explicit x:Key style for the pane GridSplitter.

        Why x:Key + splitter.Style = ... instead of an implicit
        TargetType-only style:

        - wpfui 3.1.1 does NOT register any GridSplitter style
          (verified via lepoco/wpfui repo search), so wpfui is not
          the actual reason an implicit-only style was being skipped.
        - The previous attempt left the style with only a Template
          setter (no Background setter). With the splitter relying on
          SetResourceReference at attach time, any race or transient
          null state caused the templated Border to paint nothing.
        - Binding Background as a Setter inside the style itself,
          via DynamicResource, makes the brush hot the moment the
          style is loaded — no race, no SetResourceReference call,
          and the value automatically swaps when Themes/Colors.*.xaml
          dictionaries swap.
        - Applying the style explicitly (splitter.Style = FindResource)
          bypasses the WPF implicit-style + MergedDictionary lookup
          quirks reported in dotnet/wpf #2286 and #3399.
    -->

    <Style x:Key="GhostWinGridSplitterStyle" TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource Divider.Brush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
