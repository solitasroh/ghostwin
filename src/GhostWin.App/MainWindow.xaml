<ui:FluentWindow x:Class="GhostWin.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:GhostWin.App.ViewModels"
        xmlns:controls="clr-namespace:GhostWin.App.Controls"
        Title="{Binding WindowTitle}" Width="1024" Height="768"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <ui:FluentWindow.InputBindings>
        <KeyBinding Gesture="Ctrl+T" Command="{Binding NewTabCommand}"/>
        <KeyBinding Gesture="Ctrl+W"
                    Command="{Binding CloseTabCommand}"
                    CommandParameter="{Binding SelectedTab}"/>
        <KeyBinding Gesture="Ctrl+Tab" Command="{Binding NextTabCommand}"/>
    </ui:FluentWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="120" MaxWidth="400"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TitleBar drag area -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="Transparent"/>

        <!-- Sidebar -->
        <Border Grid.Row="1" Grid.Column="0"
                Background="{ui:ThemeResource ControlFillColorDefaultBrush}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="12,8,12,8">
                    <TextBlock Text="GhostWin" FontSize="16" FontWeight="SemiBold"
                               Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                    <ui:Button Content="New Tab"
                               Icon="{ui:SymbolIcon Add24}"
                               Command="{Binding NewTabCommand}"
                               HorizontalAlignment="Stretch" Margin="0,8,0,0"/>
                </StackPanel>

                <ListBox DockPanel.Dock="Top"
                         ItemsSource="{Binding Tabs}"
                         SelectedItem="{Binding SelectedTab}"
                         HorizontalContentAlignment="Stretch"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:TerminalTabViewModel}">
                            <Grid Margin="4,6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Active indicator (dual signal: color + shape) -->
                                <Border Grid.RowSpan="2"
                                        Width="3" CornerRadius="2"
                                        Margin="0,0,8,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Background" Value="{ui:ThemeResource SystemAccentColorPrimary}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>

                                <TextBlock Grid.Column="1" Grid.Row="0"
                                           Text="{Binding Title}"
                                           FontSize="13" FontWeight="Medium"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Grid.Column="1" Grid.Row="1"
                                           Text="{Binding Cwd}"
                                           FontSize="11" Opacity="0.6"
                                           TextTrimming="CharacterEllipsis"/>

                                <ui:Button Grid.Column="2" Grid.RowSpan="2"
                                           Icon="{ui:SymbolIcon Dismiss16}"
                                           Appearance="Transparent"
                                           Command="{Binding DataContext.CloseTabCommand,
                                               RelativeSource={RelativeSource AncestorType=ListBox}}"
                                           CommandParameter="{Binding}"
                                           Width="24" Height="24"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- GridSplitter -->
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="1"
                      Background="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                      HorizontalAlignment="Center"/>

        <!-- Terminal area -->
        <Border Grid.Row="1" Grid.Column="2" Background="Black">
            <controls:TerminalHostControl x:Name="TerminalHost"
                                          RenderResizeRequested="OnTerminalResized"/>
        </Border>
    </Grid>
</ui:FluentWindow>
