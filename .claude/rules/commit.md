# Commit Rules

## Message Format
```
<type>: <short summary in English>

<optional body — what and why, not how>
```

## Types
- `feat`: new feature
- `fix`: bug fix
- `refactor`: code restructuring (no behavior change)
- `docs`: documentation only
- `chore`: build, CI, dependencies
- `test`: add or update tests

## Rules
- **English only**, lowercase start, no period at end
- Summary under 50 chars, body lines under 72 chars
- **No AI attribution** — no Co-Authored-By, no AI tool mentions, no "generated by"
- **No `--no-verify`** — fix hook failures, don't skip them
- Prefer specific file/module names over vague descriptions
- One logical change per commit

## Examples
```
feat: add vt_bridge C wrapper for ghostty API
fix: resolve Ninja lexing error on Korean locale
chore: sync ghostty submodule to upstream
docs: update ADR-003 with DLL isolation rationale
```
