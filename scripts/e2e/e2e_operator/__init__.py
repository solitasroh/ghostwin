# e2e_operator package marker
# Note: Named "e2e_operator" instead of "operator" to avoid collision with
# the Python stdlib "operator" module (C:\Python312\Lib\operator.py).
# All import sites must use "from e2e_operator.capture import ..." accordingly.
# Verified 2026-04-08: `import operator` resolves to stdlib, confirming conflict.
